{-#LANGUAGE GADTs #-}

module Data.Geometric where


{- Using Gadt so we can make Point Generic, while requiring it to be a num type  -}
data Point a where
  Point :: Num a => a -> a -> Point a



data Pointi = Pointi Int Int
type PointRd a = [a]
