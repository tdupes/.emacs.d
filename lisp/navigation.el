
(defun Go-To-End-Mark ()
  "if cursor at { it goes to its }"
  (interactive) 
  (setq       )
)


(defun opposite-of (char)
  (if (string-equal char "{") "}" )
  ()
)
(defun plus (k) (+ k 1) )
(plus 3)
(opposite-of "{")

(defun idk (bool) (if (eq bool t) "poop"))

(idk t)

(string-equal "poop" "poop")
